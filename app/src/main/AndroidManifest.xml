<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dilemma.chickenortheegg">
    <!--   android:configChanges="orientation|screenSize|screenLayout" -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".javaunits.Unit10"

            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".javaunits.Unit1"/>
        <activity android:name=".javaunits.Unit2"/>
        <activity android:name=".javaunits.Unit4"/>
        <activity android:name=".javaunits.Unit6"/>
        <activity android:name=".javaunits.Unit5"
            android:theme="@style/Theme.AppCompat.Dialog"/>
    </application>

</manifest>